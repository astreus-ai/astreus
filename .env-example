# Example environment variables for the project

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key
OPENAI_BASE_URL=https://openrouter.ai/api/v1

# OpenAI Embedding API Key (optional)
OPENAI_EMBEDDING_API_KEY=your_openai_embedding_api_key

# Ollama Configuration (optional)
# OLLAMA_BASE_URL=http://localhost:11434

# Database Configuration
# PostgreSQL settings (uncomment and configure if using PostgreSQL)
# DATABASE_TYPE=postgresql
# DATABASE_URL=postgres://user:password@localhost:5432/your_database_name

# SQLite settings (default)
DATABASE_TYPE=sqlite
DATABASE_PATH=./data/your_database.db 

# PostgreSQL with pgvector (uncomment and configure if using PostgreSQL with pgvector)
# VECTOR_DB_TYPE=postgres
# VECTOR_DB_CONNECTION_STRING=postgres://user:password@localhost:5432/vector_db

# Note: Each component (createMemory, createChat, createAgent, etc.) 
# automatically creates its required database tables when first used.
# You can customize table names using the tableNames parameter in each component. 